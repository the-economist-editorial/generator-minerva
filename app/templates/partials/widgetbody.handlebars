<h1>Ready to Rock n' Roll!</h1>
<h2>This text is in "/partials/widgetbody"<h2>
<% for(i=0;i<instances;i++) { %>
<div data-mnv="<%= jsns %>" class="mnv-widget mnv-<%= ns %>" id="<%= ns %>-<%= i %>">Instance <%= i %> of your widget</div>
<% } %>
<script>
<!--
// Config here your script budget or leave the default configuration
-->
var budgetScriptConfig = {
// list of the built-in metrics http://lafikl.github.io/perfBar/#list 
  budget: {
    // the key is the metric id
    'loadTime': {
      max: 200
    },
    'redirectCount': {
      max: 1
    },
    'globalJS': {
      min: 2,
      max: 5
    }
  }
}
</script>